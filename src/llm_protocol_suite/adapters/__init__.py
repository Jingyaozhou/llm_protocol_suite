"""Provider adapters that translate between protocol models and provider-specific formats.

Each adapter module provides ``to_<provider>_*`` and ``from_<provider>_*``
functions for bidirectional conversion.
"""
